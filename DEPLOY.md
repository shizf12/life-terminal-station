# 生命终点站 - 部署指南

## 免费部署方案对比

### 方案1：GitHub Pages（⭐ 推荐）
- ✅ 完全免费
- ✅ 最简单，无需配置
- ✅ 支持自定义域名
- ✅ 自动 HTTPS
- ⚠️ 仓库需要公开

### 方案2：Vercel
- ✅ 免费且快速
- ✅ 自动部署
- ✅ 支持自定义域名
- ⚠️ 需要注册账号

### 方案3：Netlify
- ✅ 拖拽即可部署
- ✅ 支持表单处理
- ✅ 分支预览

### 方案4：Cloudflare Pages
- ✅ 全球 CDN，速度最快
- ✅ 无限带宽
- ✅ 每天 500 次构建

---

## GitHub Pages 部署教程（推荐）

### 第一步：注册 GitHub 账号
1. 访问 https://github.com
2. 点击右上角 "Sign up"
3. 填写邮箱、密码、用户名
4. 验证邮箱

### 第二步：创建仓库
1. 登录 GitHub
2. 点击右上角 "+" → "New repository"
3. 填写仓库名：`life-terminal-station`
4. 选择 "Public"（公开）
5. 勾选 "Add a README file"
6. 点击 "Create repository"

### 第三步：上传代码

#### 方法A：网页上传（最简单）
1. 进入刚创建的仓库
2. 点击 "Add file" → "Upload files"
3. 拖拽上传以下文件：
   - `index.html`
   - `styles.css`
   - `app.js`
4. 填写提交信息："Initial commit"
5. 点击 "Commit changes"

#### 方法B：Git 命令行
```bash
# 初始化仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库（替换 username 为你的 GitHub 用户名）
git remote add origin https://github.com/username/life-terminal-station.git

# 推送代码
git push -u origin main
```

### 第四步：启用 GitHub Pages
1. 在仓库页面，点击 "Settings"（设置）
2. 左侧菜单点击 "Pages"
3. "Source" 部分选择：
   - Branch: `main`
   - Folder: `/ (root)`
4. 点击 "Save"
5. 等待 1-2 分钟

### 第五步：访问网站
- 网站地址：`https://username.github.io/life-terminal-station`
- （将 username 替换为你的 GitHub 用户名）

---

## Vercel 部署教程

### 第一步：准备代码
1. 在代码目录创建 `vercel.json` 文件：
```json
{
  "version": 2,
  "public": true,
  "github": {
    "enabled": false
  }
}
```

### 第二步：注册 Vercel
1. 访问 https://vercel.com
2. 用 GitHub 账号登录

### 第三步：部署
1. 点击 "Add New Project"
2. 选择 "Import Git Repository"
3. 授权 GitHub 并选择你的仓库
4. 点击 "Deploy"
5. 等待部署完成，获得 `.vercel.app` 域名

---

## Netlify 部署教程（最简单）

### 方法一：拖拽部署
1. 将 `index.html`、`styles.css`、`app.js` 打包成 zip
2. 访问 https://app.netlify.com/drop
3. 拖拽 zip 文件到网页
4. 自动获得 `.netlify.app` 域名

### 方法二：Git 部署
1. 注册 https://netlify.com
2. 点击 "Add new site" → "Import an existing project"
3. 连接 GitHub
4. 选择仓库，点击 "Deploy site"

---

## Cloudflare Pages 部署

1. 注册 https://dash.cloudflare.com
2. 点击 "Pages" → "Create a project"
3. 连接 GitHub 账号
4. 选择仓库
5. 框架选择 "None"
6. 点击 "Save and Deploy"

---

## 配置自定义域名（可选）

### GitHub Pages 自定义域名
1. 在仓库根目录创建 `CNAME` 文件
2. 内容填写你的域名：`yourdomain.com`
3. 在域名 DNS 添加 CNAME 记录：
   - 主机记录：`www` 或 `@`
   - 记录值：`username.github.io`

### 免费域名获取
- Freenom: https://freenom.com（.tk, .ml 等免费域名）
- Cloudflare: 注册后可使用子域名

---

## 数据备份提醒 ⚠️

**重要**：本应用使用浏览器 LocalStorage 存储数据：
- 清除浏览器数据会丢失所有记录
- 建议定期导出重要数据
- 未来可添加云同步功能

---

## 部署后优化建议

1. **添加网站图标**：创建 `favicon.ico` 文件
2. **SEO 优化**：在 HTML head 添加 meta 标签
3. **添加分析**：集成 Google Analytics 或百度统计
4. **PWA 支持**：添加 manifest.json 和 service worker

---

## 常见问题

**Q: 网站显示 404？**
A: 等待 2-5 分钟让 GitHub Pages 生效，或检查文件是否上传到根目录

**Q: 样式没有生效？**
A: 检查 CSS 文件路径是否正确，建议使用相对路径 `./styles.css`

**Q: 数据在不同设备间同步？**
A: LocalStorage 是本地存储，需要添加后端数据库才能实现同步

---

## 联系与支持

如有部署问题，可以：
1. 查看 GitHub Pages 官方文档：https://docs.github.com/zh/pages
2. 搜索错误信息
3. 在 GitHub 提交 Issue

祝部署顺利！🎉
